name: seventh_state_stream_metrics
version: 1.0.0
summary: A RabbitMQ plugin that provides metrics for RabbitMQ streams.

artifact: ../plugins/seventh_state_stream_metrics-1.0.0.ez

rabbitmq:
  # Minimum RabbitMQ version this plugin/overlay supports
  min: "4.2.x"
  # Maximum RabbitMQ version this plugin/overlay supports
  max: "4.2.x"

